/**
 * This is a placeholder file.
 * 
 * The original metals.ts config file was deleted as part of refactoring
 * where its contents were moved elsewhere in the codebase.
 * 
 * This empty file exists only to satisfy TypeScript's compilation and
 * will be removed once all references are updated.
 */

// Empty export to avoid "unused file" warnings
export { }; 